<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.javasqlitecrud">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActivityRegistrarEsc"
            android:exported="true" />
        <activity
            android:name=".ActivityRegistrarDoc"
            android:exported="true" />
        <activity android:name=".UsuarioActivity" />
        <activity android:name=".ListUsuarioActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".ActivityListaEscuela" />
        <activity android:name=".ActivityListaDocente" />
        <activity android:name=".ActivitySelection" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>